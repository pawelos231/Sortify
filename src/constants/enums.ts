export enum VisualizationSpeed {
  ULTRA_FAST = 4,
  FAST = 6,
  AVERAGE = 10,
  SLOW = 15,
  ULTRA_SLOW = 50,
}

export enum MoveType {
  swap = "swap",
  comp = "comparrison",
}

export enum Logger {
  Message = 0,
  Warn = 1,
  Error = 2,
}

export enum Algorithms {
  BUBBLE = "bubble",
  SELECTION = "selection",
  INSERTION = "insertion",
  QUICK = "quick",
  MERGE = "merge",
  HEAP = "heap",
  SHELL = "shell",
  COUNTING = "counting",
  RADIX = "radix",
  BUCKET = "bucket",
  COMB = "comb",
  PIGEONHOLE = "pigeonhole",
  CYCLE = "cycle",
  GNOME = "gnome",
}
