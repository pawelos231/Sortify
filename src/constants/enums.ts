export enum VisualizationSpeed {
  ULTRA_FAST = 4,
  FASTER = 8,
  FAST = 15,
  AVERAGE = 20,
  SLOW = 30,
  SLOWER = 50,
  ULTRA_SLOW = 250,
}

export enum MoveType {
  swap = "swap",
  comp = "comparrison",
}

export enum Logger {
  Message = 0,
  Warn = 1,
  Error = 2,
}

export enum Algorithms {
  BUBBLE = "bubble",
  SELECTION = "selection",
  INSERTION = "insertion",
  QUICK = "quick",
  MERGE = "merge",
  HEAP = "heap",
  SHELL = "shell",
  COUNTING = "counting",
  RADIX = "radix",
  BUCKET = "bucket",
  COMB = "comb",
  PIGEONHOLE = "pigeonhole",
  CYCLE = "cycle",
  GNOME = "gnome",
  BITONIC = "bitonic",
}
